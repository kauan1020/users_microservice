apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: tech-namespace
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
